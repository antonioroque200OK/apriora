# Backend Architecture

> **Status:** Placeholder — to be written in Phase 3 (Software Architecture).

This document will describe:

- NestJS module structure (`AuthModule`, `QuizModule`, `SessionModule`, `RankingModule`, `ReportModule`)
- Fastify adapter configuration
- Request/response pipeline (guards, interceptors, pipes, filters)
- Zod validation integration
- JWT authentication strategy
- Prisma service and repository pattern
- Socket.IO gateway design
