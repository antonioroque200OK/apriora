# Realtime Architecture

> **Status:** Placeholder — to be written in Phase 3 (Software Architecture).

This document will describe:

- Socket.IO event taxonomy (client → server, server → client)
- Room strategy (session rooms, teacher room, student rooms)
- Live session lifecycle (create, start, question, answer, score, end)
- Reconnection handling
- Presence and participant management
- Mobile client integration (React Native + Socket.IO)
