# Database Architecture

> **Status:** Placeholder — to be written in Phase 3 (Software Architecture).

This document will describe:

- PostgreSQL schema overview
- Prisma data models (User, Quiz, Question, Option, Session, Participant, Answer, Score)
- Indexing strategy
- Migration workflow (`prisma migrate dev`, `prisma migrate deploy`)
- Neon PostgreSQL configuration (branching, connection pooling)
- Seed data strategy
